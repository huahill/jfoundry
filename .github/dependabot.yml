version: 2
updates:
  # Dependabot's Maven fetcher follows <modules> only. This repository uses Maven 4.1
  # <subprojects>, so BOM manifests must be listed explicitly. Runtime aggregators are
  # omitted because they use inferred parents and ${project.version}, which Dependabot
  # cannot evaluate.
  - package-ecosystem: maven
    directories:
      - "/"
      - "/jfoundry-boms/*"
    schedule:
      interval: daily
    cooldown:
      default-days: 1
      semver-major-days: 7
      semver-minor-days: 7
      semver-patch-days: 1
      include:
        - org.springframework.boot:spring-boot-dependencies
        - org.springframework.boot:spring-boot-starter-parent
        - org.springframework.boot:spring-boot-maven-plugin
        - io.quarkus.platform:quarkus-bom
        - io.quarkus:quarkus-extension-maven-plugin
        - io.quarkus:quarkus-extension-processor
        - io.quarkus:quarkus-maven-plugin
      exclude:
        - org.springframework.boot:spring-boot-dependencies
        - org.springframework.boot:spring-boot-starter-parent
        - org.springframework.boot:spring-boot-maven-plugin
    open-pull-requests-limit: 10
    rebase-strategy: disabled
    groups:
      jfoundry-spring-boot-platform:
        patterns:
          - org.springframework.boot:spring-boot-dependencies
          - org.springframework.boot:spring-boot-starter-parent
          - org.springframework.boot:spring-boot-maven-plugin
        update-types: [patch, minor]
      jfoundry-quarkus-platform:
        patterns:
          - io.quarkus.platform:quarkus-bom
          - io.quarkus:quarkus-extension-maven-plugin
          - io.quarkus:quarkus-extension-processor
          - io.quarkus:quarkus-maven-plugin
        update-types: [patch, minor]
      jfoundry-maven-patches:
        patterns: ["*"]
        update-types: [patch]
      jfoundry-maven-minors:
        patterns: ["*"]
        update-types: [minor]
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 10
    groups:
      github-codeql-action:
        patterns: [github/codeql-action/*]

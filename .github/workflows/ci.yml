name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - production-readiness-release
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  changes:
    name: Classify changes
    runs-on: ubuntu-latest
    outputs:
      run_full: ${{ steps.scope.outputs.run_full }}
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Classify changed paths
        id: scope
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_BEFORE: ${{ github.event.before }}
          PULL_REQUEST_BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail

          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            echo "run_full=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          base="$EVENT_BEFORE"
          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            base="$PULL_REQUEST_BASE"
          fi
          if [[ "$base" =~ ^0+$ ]]; then
            base="$(git rev-list --max-parents=0 HEAD | tail -n 1)"
          fi

          run_full=false
          while IFS= read -r changed_path; do
            case "$changed_path" in
              README.md|README_ZH.md|AGENTS.md|docs/*)
                ;;
              *)
                run_full=true
                break
                ;;
            esac
          done < <(git diff --name-only "$base" "$GITHUB_SHA")

          echo "run_full=$run_full" >> "$GITHUB_OUTPUT"

  docs:
    name: Documentation checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Verify documentation
        run: bash scripts/verify-docs.sh

      - name: Verify release workflow
        run: bash scripts/verify-release-workflow.sh

      - name: Verify Spring Boot Parent remediation workflow
        run: bash scripts/verify-spring-boot-parent-remediation-workflow.sh

      - name: Verify release POM metadata
        run: bash scripts/verify-release-pom-metadata.sh

      - name: Verify release SBOM configuration
        run: bash scripts/verify-release-sbom.sh

      - name: Verify supply-chain workflows
        run: bash scripts/verify-supply-chain-workflows.sh

      - name: Verify workflow path policy
        run: bash scripts/verify-workflow-paths.sh

      - name: Test Consumer POM verification
        run: bash scripts/verify-consumer-pom-test.sh

      - name: Test release POM metadata verification
        run: bash scripts/verify-release-pom-metadata-test.sh

      - name: Test release governance verifiers
        run: |
          bash scripts/verify-release-workflow-test.sh
          bash scripts/verify-spring-boot-parent-remediation-workflow-test.sh
          bash scripts/verify-release-sbom-test.sh
          bash scripts/verify-supply-chain-workflows-test.sh
          bash scripts/verify-workflow-paths-test.sh

  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Review dependency changes
        uses: actions/dependency-review-action@2031cfc080254a8a887f58cffee85186f0e49e48 # v4
        with:
          fail-on-severity: high
          deny-licenses: AGPL-3.0, GPL-2.0-only, GPL-3.0-only, SSPL-1.0

  test:
    name: Test (Java ${{ matrix.java }})
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.run_full == 'true'
    strategy:
      fail-fast: false
      matrix:
        java:
          - 25
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Java
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
          cache: maven

      - name: Verify dependency boundaries
        run: bash scripts/verify-dependency-boundaries.sh

      - name: Test dependency boundary verifier
        run: bash scripts/verify-dependency-boundaries-test.sh

      - name: Test Mason YAML build tooling
        run: |
          bash scripts/convert-mason-poms-test.sh
          bash scripts/convert-mason-reactor-test.sh
          bash scripts/set-mason-reactor-version-test.sh
          bash scripts/verify-mason-poc-test.sh
          bash scripts/verify-mason-poc.sh

      - name: Run tests
        run: ./mvnw -B test

  package:
    name: Package artifacts
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Java
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: 25
          cache: maven

      - name: Package without tests
        run: ./mvnw -B -DskipTests package

      - name: Verify compiler processor artifacts
        run: scripts/verify-maven-compiler-processor-artifacts.sh

  spring-integration:
    name: Spring middleware integration
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Java
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: 25
          cache: maven

      - name: Run Spring middleware integration tests
        run: JAVA_25_HOME="$JAVA_HOME" bash scripts/verify-runtime-ci.sh spring --stage middleware

  quarkus-integration:
    name: Quarkus middleware integration
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Java
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: 25
          cache: maven

      - name: Run Quarkus PostgreSQL integration tests
        run: JAVA_25_HOME="$JAVA_HOME" bash scripts/verify-runtime-ci.sh quarkus --stage middleware

  helidon-integration:
    name: Helidon middleware integration
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Java
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: 25
          cache: maven

      - name: Run Helidon PostgreSQL integration tests
        run: JAVA_25_HOME="$JAVA_HOME" bash scripts/verify-runtime-ci.sh helidon --stage middleware

  spring-native:
    name: Spring Native Image
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up GraalVM
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: graalvm
          java-version: 25
          cache: maven

      - name: Build and smoke test Spring Native Image
        run: GRAALVM_HOME="$JAVA_HOME" bash scripts/verify-runtime-ci.sh spring --stage native

  spring-native-mybatis-plus:
    name: Spring Native Image (MyBatis-Plus)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up GraalVM
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: graalvm
          java-version: 25
          cache: maven

      - name: Build and verify MyBatis-Plus Spring Native Image
        run: GRAALVM_HOME="$JAVA_HOME" bash scripts/verify-runtime-ci.sh spring --stage native-mybatis-plus

  spring-native-redisson:
    name: Spring Native Image (Redisson)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up GraalVM
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: graalvm
          java-version: 25
          cache: maven

      - name: Build and verify Redisson Spring Native Image
        run: GRAALVM_HOME="$JAVA_HOME" bash scripts/verify-runtime-ci.sh spring --stage native-redisson

  spring-native-jobrunr:
    name: Spring Native Image (JobRunr)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up GraalVM
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: graalvm
          java-version: 25
          cache: maven

      - name: Build and verify JobRunr Spring Native Image
        run: GRAALVM_HOME="$JAVA_HOME" bash scripts/verify-runtime-ci.sh spring --stage native-jobrunr

  quarkus-native:
    name: Quarkus Native Image
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Java
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: 25
          cache: maven

      - name: Run Quarkus Native Image smoke test
        run: JAVA_25_HOME="$JAVA_HOME" bash scripts/verify-runtime-ci.sh quarkus --stage native

  helidon-native:
    name: Helidon Native Image
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up GraalVM
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: graalvm
          java-version: 25
          cache: maven

      - name: Build and smoke test Helidon Native Image
        run: GRAALVM_HOME="$JAVA_HOME" bash scripts/verify-runtime-ci.sh helidon --stage native

  maven-4:
    name: Maven 4 compatibility
    runs-on: ubuntu-latest
    needs: package
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Java
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: 25
          cache: maven

      - name: Check Maven 4 package compatibility
        run: ./mvnw -B -DskipTests package

      - name: Verify reactor Consumer POMs
        run: |
          set -euo pipefail

          consumer_pom_repository="${RUNNER_TEMP}/jfoundry-consumer-pom-repository"
          ./mvnw -B -DskipTests -Dmaven.repo.local="${consumer_pom_repository}" install
          version="$(
            ./mvnw -q help:evaluate -Dexpression=project.version -DforceStdout |
              sed -n 's/^\[INFO\] \[stdout\] //p' |
              tail -n 1
          )"
          bash scripts/verify-consumer-pom.sh "${consumer_pom_repository}" "${version}" \
            "$(pwd)/mvnw"

  merge-gate:
    name: Merge gate
    runs-on: ubuntu-latest
    needs:
      - changes
      - docs
      - dependency-review
      - test
      - package
      - spring-integration
      - quarkus-integration
      - helidon-integration
      - spring-native
      - spring-native-mybatis-plus
      - spring-native-redisson
      - spring-native-jobrunr
      - quarkus-native
      - helidon-native
      - maven-4
    if: ${{ always() }}
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Test merge requirements evaluator
        run: bash scripts/verify-merge-gate-test.sh

      - name: Verify merge requirements
        run: >-
          bash scripts/verify-merge-gate.sh
          "${{ needs.changes.outputs.run_full }}"
          "${{ github.event_name == 'pull_request' }}"
          "${{ needs.docs.result }}"
          "${{ needs.dependency-review.result }}"
          "${{ needs.test.result }}"
          "${{ needs.package.result }}"
          "${{ needs.spring-integration.result }}"
          "${{ needs.quarkus-integration.result }}"
          "${{ needs.helidon-integration.result }}"
          "${{ needs.spring-native.result }}"
          "${{ needs.spring-native-mybatis-plus.result }}"
          "${{ needs.spring-native-redisson.result }}"
          "${{ needs.spring-native-jobrunr.result }}"
          "${{ needs.quarkus-native.result }}"
          "${{ needs.helidon-native.result }}"
          "${{ needs.maven-4.result }}"

modelVersion: 4.0.0
parent:
  groupId: io.github.xfoundries
  artifactId: jfoundry-quarkus
  version: 1.5.0-SNAPSHOT
  relativePath: ../../pom.yaml
artifactId: jfoundry-transaction-quarkus-runtime
name: jfoundry-transaction-quarkus-runtime
description: "Quarkus runtime adapter for JFoundry application transactions."
dependencies:
  - groupId: io.github.xfoundries
    artifactId: jfoundry-transaction-core
  - groupId: io.github.xfoundries
    artifactId: jfoundry-transaction-jta
  - groupId: io.quarkus
    artifactId: quarkus-arc
  - groupId: io.quarkus
    artifactId: quarkus-narayana-jta
  - groupId: org.junit.jupiter
    artifactId: junit-jupiter
    scope: test
  - groupId: org.assertj
    artifactId: assertj-core
    scope: test
  - groupId: io.quarkus
    artifactId: quarkus-junit
    scope: test
build:
  plugins:
    - groupId: io.quarkus
      artifactId: quarkus-maven-plugin
      version: ${quarkus.version}
      extensions: "true"
      configuration:
        skipSourceGeneration: ${skipTests}
      executions:
        - goals:
            - generate-code-tests
    - groupId: io.quarkus
      artifactId: quarkus-extension-maven-plugin
      version: ${quarkus.version}
      executions:
        - phase: compile
          goals:
            - extension-descriptor
          configuration:
            deployment: "${project.groupId}:jfoundry-transaction-quarkus-deployment:${project.version}"

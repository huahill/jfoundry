modelVersion: 4.0.0
parent:
  groupId: io.github.xfoundries
  artifactId: jfoundry-quarkus
  version: 1.5.0-SNAPSHOT
  relativePath: ../../pom.yaml
artifactId: jfoundry-persistence-jpa-quarkus-runtime
name: jfoundry-persistence-jpa-quarkus-runtime
description: "Quarkus runtime assembly for JFoundry JPA persistence failure translation."
dependencies:
  - groupId: io.github.xfoundries
    artifactId: jfoundry-persistence-quarkus-runtime
  - groupId: io.github.xfoundries
    artifactId: jfoundry-persistence-jpa
  - groupId: io.quarkus
    artifactId: quarkus-arc
  - groupId: io.quarkus
    artifactId: quarkus-hibernate-orm
  - groupId: org.junit.jupiter
    artifactId: junit-jupiter
    scope: test
  - groupId: org.assertj
    artifactId: assertj-core
    scope: test
build:
  plugins:
    - groupId: io.quarkus
      artifactId: quarkus-extension-maven-plugin
      version: ${quarkus.version}
      executions:
        - phase: compile
          goals:
            - extension-descriptor
          configuration:
            deployment: "${project.groupId}:jfoundry-persistence-jpa-quarkus-deployment:${project.version}"

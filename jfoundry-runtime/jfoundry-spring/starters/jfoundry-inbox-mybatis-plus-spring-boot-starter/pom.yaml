modelVersion: 4.0.0
parent:
  groupId: io.github.xfoundries
  artifactId: jfoundry-spring
  version: 1.4.0-SNAPSHOT
  relativePath: ../../pom.yaml
artifactId: jfoundry-inbox-mybatis-plus-spring-boot-starter
name: jfoundry-inbox-mybatis-plus-spring-boot-starter
description: "Spring Boot starter for the MyBatis-Plus InboxMessageStore adapter."
dependencies:
  - groupId: io.github.xfoundries
    artifactId: jfoundry-inbox-spring-boot-starter
  - groupId: com.baomidou
    artifactId: mybatis-plus-spring-boot4-starter
  - groupId: io.github.xfoundries
    artifactId: jfoundry-inbox-mybatis-plus
build:
  plugins:
    - groupId: org.apache.maven.plugins
      artifactId: maven-javadoc-plugin
      configuration:
        skip: "true"
    - groupId: org.apache.maven.plugins
      artifactId: maven-jar-plugin
      executions:
        - id: attach-empty-javadoc
          phase: package
          goals:
            - jar
          configuration:
            classesDirectory: ${maven.multiModuleProjectDirectory}/.mvn/javadoc-placeholder
            classifier: javadoc
            includes:
              - README.md
